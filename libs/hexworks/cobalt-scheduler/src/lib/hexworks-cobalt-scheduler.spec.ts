import { hexworksCobaltScheduler } from "./hexworks-cobalt-scheduler";

describe("hexworksCobaltScheduler", () => {
    it("should work", () => {
        expect(hexworksCobaltScheduler()).toEqual("hexworks-cobalt-scheduler");
    });
});
